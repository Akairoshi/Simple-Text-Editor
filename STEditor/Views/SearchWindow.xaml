<Window x:Class="STEditor.Views.SearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:STEditor.Views"
        mc:Ignorable="d"
        Title="SearchWindow" Height="200" Width="500" ResizeMode="NoResize" Icon="/Assets/Icons/AppIcon.ico">
    <Grid Margin="10,10,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="43*"/>
            <RowDefinition Height="121*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal">
            <StackPanel Orientation="Horizontal" Width="389" Height="40">
                <TextBlock TextWrapping="Wrap" Text="Query:" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="15,0,0,0"/>
                <TextBox Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" Width="292" Height="24" Margin="15,0,0,0"/>
            </StackPanel>
            <Button Content="Search" Command="{Binding FindCommand}"  Width="76" Height="24"/>
        </StackPanel>
        <DockPanel Grid.Row="1" VerticalAlignment="Center" >
            <StackPanel Width="258">
                <TextBlock TextWrapping="Wrap" Text="Search mode" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <RadioButton Content="Find" GroupName="FindMode" Command="{Binding SetFindModeCommand}" CommandParameter="All" Margin="0,0,0,5" Width="120"/>
                <RadioButton Content="Find next" GroupName="FindMode" Command="{Binding SetFindModeCommand}" CommandParameter="Next" Margin="0,0,0,5" Width="120"/>
                <RadioButton Content="Find previous" GroupName="FindMode"  Command="{Binding SetFindModeCommand}" CommandParameter="Previous" Margin="0,0,0,5" Width="120"/>
            </StackPanel>
            <StackPanel Width="168">
                <TextBlock TextWrapping="Wrap" VerticalAlignment="Center" HorizontalAlignment="Center" Text="Status"/>
                <TextBlock TextWrapping="Wrap" Text="{Binding SearchStatus}" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,20,0,0" FontStyle="Italic" FontSize="22" FontWeight="Bold"/>
            </StackPanel>
        </DockPanel>

    </Grid>
</Window>
